var isArmstrong = function(n){
	"use strict"; var sum = 0
	n = String(n);
for(var i = 0;
 	i < n.length;
 	++i);
 	var pwr=Math.pow();
sum = sum + pwr
if(sum == n){
	return true
	} else {
		return false
};
};
